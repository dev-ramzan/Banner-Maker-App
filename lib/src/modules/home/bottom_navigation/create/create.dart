import 'package:flutter/material.dart';

class CreateBanner extends StatelessWidget {
  const CreateBanner({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
        child: Text('Create Banner sheeet'),
      ),
    );
  }
}
