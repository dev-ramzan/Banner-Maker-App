import 'package:flutter/material.dart';

class SearchBanners extends StatelessWidget {
  const SearchBanners({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.amber,
        child: Text('Search banner sheeet'),
      ),
    );
  }
}
